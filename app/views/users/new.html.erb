<p id="notice"><%= notice %></p>
<%= render 'static/nav' %>

<div class="user-form">
	<%= form_for(@user) do |form| %>
		<%= render form %>

		<div class="actions">
		  <%= form.submit 'Sign Up' %>
		</div>

	<% end %>
</div>